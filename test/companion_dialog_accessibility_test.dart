import 'package:flutter/material.dart';
import 'package:testing/AccessibilityTest.dart';

class CompanionDialogAccess_T /

  const Builder builder = com of(
    <WindowContext width=100, height=100>
      <CompanionBuilder(
        isTypical: yes,
        isSpecial: no,
          parentProviderKey: "companion_dialog_access"
      ) />
    </WindowContext>
  />
).pr();

if ( ongoing) builder.start();